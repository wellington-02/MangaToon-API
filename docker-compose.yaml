services:
  postgres:
    image: postgres:latest
    container_name: mangaToon-container
    environment:
      POSTGRES_DB: mangatoonbd
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data2

volumes:
  postgres_data: